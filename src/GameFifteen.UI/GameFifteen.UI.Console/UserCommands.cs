namespace GameFifteen.UI.Console
{
    internal enum UserCommands
    {
        // Workaround for bug with parsing enums
        Exit = 300,
        Restart = 301,
        Top = 302,
        Undo = 303,
        Redo = 304
    }
}